<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.model.mapper.Food">
	<select id="selectAll" resultType="food">
		select * from food
	</select>

	<select id="selectMyFoodAll" resultType="food" parameterType="String">
		select * from food f join takenfood t on t.user_email = #{email} where f.code = t.food_code
	</select>
	<select id="selectCode" resultType="food" parameterType="int">
		select * from food where code=#{code}
	</select>
	<select id="selectMaker" resultType="food" parameterType="String">
	 <bind name="pattern" value="'%' + _parameter + '%'" />
		select * from food where maker like #{pattern}
	</select>
	<select id="selectMaterial" resultType="food" parameterType="String">
		<bind name="pattern" value="'%' + _parameter + '%'" />
		select * from food where material like #{pattern}
	</select>
	
	<select id="selectName" resultType="food" parameterType="String">
		<bind name="pattern" value="'%' + _parameter + '%'" />
		select * from food where name like #{pattern}
	</select>
	
	<select id="selectMyFood" resultType="food" parameterType="map">
		select * from food f join takenfood t on t.user_email = #{email} where f.code = #{code}
	</select>
	
	<insert id="insertMyFood" parameterType="map">
		insert into takenfood (user_email, food_code, quantity) values(#{email},#{code},#{quantity})
	</insert>

	<update id="updateMyFood" parameterType="map">
		update takenfood set quantity = #{quantity} where user_email =#{email} and food_code=#{code}
	</update>
	<delete id="deleteMyfood" parameterType="map">
		delete from takenfood where  user_email =  #{email} and food_code = #{code}
	</delete>
</mapper>