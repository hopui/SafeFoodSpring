<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.model.mapper.UserInfo">
	<select id="selectAllUsers" resultType="userInfo">
		select * from user_info
	</select>

	<select id="select" resultType="userInfo" parameterType="string">
		select * from user_info where user_id=#{id}
	</select>

	<insert id="insert" parameterType="userInfo">
		insert into user_info values(#{userId}, #{name}, #{password})
	</insert>

	<update id="update" parameterType="userInfo">
		update user_info set name=#{name}, password=#{password} where user_id=#{userId}
	</update>
	<delete id="delete" parameterType="string">
		delete from user_info where user_id=#{userId}
	</delete>
</mapper>